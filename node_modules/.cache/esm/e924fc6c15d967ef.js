let arg,inquirer,createProject;_15d‍.x([["cli",()=>cli]]);_15d‍.w("arg",[["default",["arg"],function(v){arg=v}]]);_15d‍.w("inquirer",[["default",["inquirer"],function(v){inquirer=v}]]);_15d‍.w("./main",[["createProject",["createProject"],function(v){createProject=v}]]);




//creates commands/arguments users can pass in
function parseCommandLineOptions(rawArgs) {
    const args = arg(
        {
            '--git': Boolean,
            '--yes': Boolean,
            '--intsall': Boolean,
            '-g': '--git',
            '-y': '--yes',
            '-i': '--install'
        },
        {
            argv: rawArgs.slice(2)
        }
    );
    return {
        skipPrompts: args['--yes'] || false,
        git: args['--git'] || false,
        runInstall: args['--install'] || false,
        template: args._[0],
    }
}

async function promptUsersForMissingInputs(options) {

    //account for missing file template
    const defaultTemplate = 'Javascript';
    if (options.skipPrompts) {
        return {
            ...options,
            template: options.template || defaultTemplate
        }
    }

    const questions = [];

    if (!options.template) {
        questions.push({
            type: 'list',
            name: 'template',
            message: 'Please choose which template to use',
            choices: ['Javascript', 'Typescript'],
            default: defaultTemplate
        })
    };

    //account for missing git command
    if (!options.git) {
        questions.push({
            type: 'confirm',
            name: 'git',
            message: 'initalise a git repository?',
            default: 'false'
        })
    }

    const answers = await inquirer.prompt(questions);
    return {
        ...options,
        template: options.template || answers.template,
        git: options.git || answers.git
    }

}


//logs cli questions to the console
       async function cli(args) {
    let options = parseCommandLineOptions(args)
    options = await promptUsersForMissingInputs(options)
    await createProject(options);
    _15d‍.g.console.log(options)
}
